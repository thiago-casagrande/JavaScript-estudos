const readline = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout
});

let carros = ["gol", "civic", "corolla", "HB20", "onix"];
console.log(carros[0], carros[4]);
